import { SearchStatusFilterPipe } from './search-status-filter.pipe';

describe('SearchStatusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStatusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
