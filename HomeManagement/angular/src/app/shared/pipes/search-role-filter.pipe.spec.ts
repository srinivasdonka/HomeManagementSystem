import { SearchRoleFilterPipe } from './search-role-filter.pipe';

describe('SearchRoleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRoleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
